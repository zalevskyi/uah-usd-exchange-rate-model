CREATE TABLE data (quote_date TEXT UNIQUE NOT NULL,
                   uah_usd REAL,
                   uah_eur REAL,
                   uah_rub REAL,
                   uah_pln REAL,
                   brent REAL,
                   wheat REAL,
                   steal REAL,
                   mvemsd REAL)